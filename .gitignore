__pycache__/
*.pyc
*.egg-info/
.venv/
build/
dist/
.pytest_cache/
*.db
.lbrain/
.env
.env.*
!.env.example

# A user's own corpus is theirs and must never be committed. LBRAIN_HOME can
# relocate the config dir — API keys included — into a working tree, and the
# default lair/memory roots can be created inside one too.
lairs/
memory/
